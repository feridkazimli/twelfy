import { generateQuestion } from "./gamesBefore";
import { getQuestionForUser, sendAnswer } from "./playGame";
import { checkAnswer } from "./gamesAfter";

export default {
    generateQuestion,
    getQuestionForUser, sendAnswer,
    checkAnswer,
}